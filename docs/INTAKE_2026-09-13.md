# Work-thread repository intake — 13 September 2026

## Inspected source
- Default branch main: `03b31f1ed4dfa3508ea9e1aeacb85cc19520aa9c`, merge of PR #26.
- Only open PR: #27, `feat/phase2-settings-appearance`, initially `42fc9dfdceaa5aa1b260a903e712c4ee3b96d94a`.
- All 27 PR records, remote branch tips, recent commit history and open issues inspected. No open issues. Historical topic branches were retained after merges, often squash merges; lack of ancestry alone is not unfinished work. PR #10 closed unmerged; PR #6/#8 have documented physical rejection/recovery history. Do not revive these implementations.
- main's PROJECT_STATE still described PR #26 as pending. PR #27 already carries the accepted restart evidence and updated state; continue that PR instead of duplicating Settings work.

## Canonical documents
| Concern | Source |
| --- | --- |
| Current status / exact next step | PROJECT_STATE.md |
| Scope / constraints | PRODUCT.md |
| Roadmap | BUILD_SPEC.md, current phase sequence in PROJECT_STATE.md |
| Architecture | ARCHITECTURE.md, decisions/0001–0006 |
| UX / visual baseline | UX.md, DESIGN_SYSTEM.md |
| Data boundary | DATA.md |
| Delivery history / physical evidence | DEVLOG.md, timestamped evidence documents |

## Implemented reality
Expo 57 / React Native 0.86.2 / React 19.2.3 / TypeScript. Three Guide surfaces share Teevee-owned programme types and deterministic 48-channel runtime fixtures. Calendar semantics, DST, programme boundaries and resume refresh have automated coverage. Totaal uses UI-thread horizontal position without per-frame JS bridging; Per zender uses a three-channel pager retaining vertical time; Nu & Straks keeps reference-time state separate from native rail momentum. Programme Detail uses a native modal and gesture root with close/back/escape/swipe wiring. Guide preference storage and navigation are implemented. Search and Tonight remain placeholders.

PR #27 adds live appearance context, persisted System/Light/Dark choice and Settings. Its initial native Android build succeeded, but quality stopped at a colour-scheme type error before lint/tests/exports. Local review must therefore execute those previously skipped checks too.

## Documentation discrepancies reconciled
- Phase 1B and rail-regression documents still presented closed interaction gates as active/open.
- Per-channel implementation/testing text still assumed a temporary switch/default.
- Reintegration wording omitted accepted persisted deferred restore.
- Testing described web-only export despite native bundle and Gradle CI gates.
- Architecture did not distinguish implemented foundations from proposed server/cache libraries.
- Programme Detail's approved actions were described beside implemented capabilities without explicitly marking them unbuilt. The action/sticky treatment, title-first hierarchy, scalable long-description layout and production safe-area polish remain delivery work; this intake does not redesign or silently claim them complete.

## Risks and missing delivery
- Physical Android interaction/Back/performance acceptance remains deferred. This runtime has no adb/emulator executable; cloud CI provides compilation, not device evidence.
- Preserve Nu & Straks dynamic import after shell startup; prior static-import integration failed physically despite green CI.
- Large-text/native layout and Settings return flow require focused validation.
- Current Guide header/layout implementations remain partly duplicated; extract only where measured/repeated needs justify it.
- Production schedule/API/cache, provider and artwork rights, notifications/saved programmes, subscriptions, observability and licensed typography are not implemented.
- Historical moderate dependency advisories require targeted analysis; no forced upgrades.

## Next decision
Complete existing PR #27, repair quality failures, test appearance integration and return navigation, reconcile docs, then verify both CI jobs at the exact repaired head before merging. Perform the small required iPhone appearance smoke afterwards. This establishes the shared theme foundation before further shell/error-boundary work. The roadmap already moved all three Guide interaction prototypes before shell/data work; no further phase reorder is justified by this intake.
